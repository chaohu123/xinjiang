# 快速启动指南

## 🚀 5分钟快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 配置环境变量（可选）

创建 `.env` 文件：

```env
VITE_API_BASE_URL=http://localhost:8080/api
```

### 3. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

### 4. 构建生产版本

```bash
npm run build
```

构建产物在 `dist` 目录

## 📋 常用命令

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint

# 代码格式化
npm run format
```

## 🔧 开发建议

### 1. 使用 VS Code 插件

推荐安装以下插件：
- Volar (Vue 3 支持)
- ESLint
- Prettier
- TypeScript Vue Plugin

### 2. 代码规范

项目已配置 ESLint 和 Prettier，建议：
- 保存时自动格式化
- 提交前运行 `npm run lint`

### 3. API 对接

所有 API 接口在 `src/api/` 目录下，需要根据后端实际接口调整：
- 接口路径
- 请求参数
- 响应数据格式

### 4. 样式开发

- 使用 SCSS 编写组件样式
- 使用 Tailwind CSS 进行快速样式开发
- 遵循 Element Plus 设计规范

## 🐛 常见问题

### Q: 启动后页面空白？
A: 检查控制台错误，可能是 API 接口问题，开发环境会代理到后端

### Q: 路由跳转404？
A: 确保后端支持前端路由的 history 模式，或使用 hash 模式

### Q: 图片不显示？
A: 检查图片路径，确保图片资源在 `public` 目录或正确配置了资源路径

### Q: API 请求失败？
A: 检查后端服务是否启动，以及 `vite.config.ts` 中的代理配置

## 📚 相关文档

- [Vue 3 文档](https://vuejs.org/)
- [Vite 文档](https://vitejs.dev/)
- [Element Plus 文档](https://element-plus.org/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [Vue Router 文档](https://router.vuejs.org/)

## 💡 提示

- 开发时建议使用 Chrome DevTools 进行调试
- 使用 Vue DevTools 插件查看组件状态
- 网络请求可在 Network 面板查看
- 状态管理可在 Pinia DevTools 中查看






