# 新疆数字文化平台 — 前端 (frontend)

## 📘 项目简介

新疆数字文化平台前端基于 **Vue 3 + Vite** 开发，面向游客、研究者及文化机构用户，提供新疆地区的数字文化展示与交互体验，包括文章、展品、视频、地图、活动与社区等功能。

系统采用 **模块化 + 响应式设计**，兼顾桌面端与移动端体验，支持多语言（中文/英文），与 Spring Boot 后端 REST API 对接，实现数据交互与用户认证。

- 管理员后台入口：`/admin`（需管理员权限）

---

## ⚙️ 技术栈

| 类别 | 技术 |
|------|------|
| 前端框架 | Vue 3 (Composition API) |
| 构建工具 | Vite 5.2.0 |
| 组件库 | Element Plus 2.6.3 |
| 路由管理 | Vue Router 4.3.0 |
| 状态管理 | Pinia 2.1.7 |
| 国际化 | vue-i18n 9.9.1 |
| 网络请求 | Axios 1.6.7（带 JWT 拦截器） |
| 地图服务 | Mapbox GL 3.1.2 |
| 多媒体展示 | Video.js 8.10.0、PhotoSwipe 5.4.2、Three.js 0.162.0（360° 展示） |
| 样式 | SCSS + Tailwind CSS 3.4.1 |
| 工具库 | dayjs 1.11.10、@vueuse/core 10.9.0 |
| 进度条 | nprogress 0.2.0 |
| 测试 | Vitest 1.3.1 + Vue Testing Library |
| 代码规范 | ESLint + Prettier |
| 类型检查 | TypeScript 5.5.4 |

---

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 7.0.0 或 yarn >= 1.22.0

### 安装依赖

```bash
npm install
# 或
yarn install
```

### 开发模式

```bash
npm run dev
# 或
yarn dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
# 或
yarn build
```

### 预览生产构建

```bash
npm run preview
# 或
yarn preview
```

---

## 📁 项目结构

```
frontend/
├── src/
│   ├── api/              # API 接口封装
│   │   ├── admin.ts      # 管理员接口
│   │   ├── carousel.ts   # 轮播图接口
│   │   ├── community.ts  # 社区接口
│   │   ├── culture.ts    # 文化资源接口
│   │   ├── event.ts      # 活动接口
│   │   ├── route.ts      # 路线接口
│   │   └── user.ts       # 用户接口
│   │
│   ├── components/       # 公共组件
│   │   ├── admin/        # 管理员组件
│   │   │   ├── CarouselManagement.vue
│   │   │   ├── CultureManagement.vue
│   │   │   ├── EventManagement.vue
│   │   │   ├── HomeRecommendationManagement.vue
│   │   │   ├── PostManagement.vue
│   │   │   └── UserManagement.vue
│   │   ├── common/       # 通用组件
│   │   │   ├── BackToTop.vue
│   │   │   ├── Carousel.vue
│   │   │   ├── CultureCard.vue
│   │   │   ├── EmptyState.vue
│   │   │   ├── ImageGallery.vue
│   │   │   ├── LikeButton.vue
│   │   │   ├── LoadingSpinner.vue
│   │   │   └── VideoPlayer.vue
│   │   └── layout/       # 布局组件
│   │       ├── Footer.vue
│   │       └── Header.vue
│   │
│   ├── i18n/             # 国际化配置
│   │   ├── index.ts
│   │   └── locales/
│   │       ├── en.ts     # 英文
│   │       └── zh.ts     # 中文
│   │
│   ├── layouts/          # 页面布局
│   │   ├── AdminLayout.vue    # 管理员布局
│   │   └── MainLayout.vue     # 主布局
│   │
│   ├── router/           # 路由配置
│   │   └── index.ts
│   │
│   ├── store/            # Pinia 状态管理
│   │   ├── app.ts        # 应用状态
│   │   ├── user.ts       # 用户状态
│   │   └── index.ts
│   │
│   ├── styles/           # 全局样式
│   │   └── main.scss
│   │
│   ├── types/            # TypeScript 类型定义
│   │   ├── carousel.ts
│   │   ├── community.ts
│   │   ├── culture.ts
│   │   ├── event.ts
│   │   ├── route.ts
│   │   └── user.ts
│   │
│   ├── utils/            # 工具函数
│   │   ├── auth.ts       # 认证工具
│   │   ├── axios.ts      # Axios 配置
│   │   ├── constants.ts  # 常量
│   │   ├── index.ts      # 通用工具
│   │   └── permission.ts # 权限工具
│   │
│   ├── views/            # 页面组件
│   │   ├── admin/        # 管理员页面
│   │   │   ├── AdminCarousels.vue
│   │   │   ├── AdminCulture.vue
│   │   │   ├── AdminDashboard.vue
│   │   │   ├── AdminEvents.vue
│   │   │   ├── AdminPosts.vue
│   │   │   ├── AdminRecommendations.vue
│   │   │   └── AdminUsers.vue
│   │   ├── Admin.vue
│   │   ├── Community.vue
│   │   ├── CommunityPost.vue
│   │   ├── Detail.vue
│   │   ├── EventDetail.vue
│   │   ├── Events.vue
│   │   ├── Home.vue
│   │   ├── Login.vue
│   │   ├── Map.vue
│   │   ├── NotFound.vue
│   │   ├── Profile.vue
│   │   ├── Register.vue
│   │   ├── RouteDetail.vue
│   │   ├── Routes.vue
│   │   └── Search.vue
│   │
│   ├── App.vue           # 根组件
│   └── main.ts           # 入口文件
│
├── public/               # 公共静态资源
├── index.html            # HTML 模板
├── vite.config.ts        # Vite 配置
├── tsconfig.json         # TypeScript 配置
├── tailwind.config.js    # Tailwind 配置
├── postcss.config.js     # PostCSS 配置
└── package.json          # 项目配置
```

---

## 🧭 功能说明

### 用户端主要功能

#### 1. 首页（/home）
- **轮播图展示**：展示文化主题轮播图
- **快速导航**：地图探索、路线推荐、活动日历、社区互动
- **精选推荐**：管理员配置的精选文化资源
- **热门资源**：基于浏览量的热门文化资源
- **最新活动**：最新发布的文化活动

#### 2. 搜索（/search）
- **关键词搜索**：支持文化资源标题、描述搜索
- **多维度筛选**：
  - 类型筛选：文章、展品、视频等
  - 地区筛选：按地区筛选资源
  - 标签筛选：按标签筛选资源
- **分页展示**：支持分页浏览搜索结果

#### 3. 详情页（/detail/:type/:id）
- **资源详情**：展示文化资源完整信息
- **多媒体展示**：
  - 图片画廊：支持多图浏览
  - 视频播放：Video.js 视频播放器
  - 音频播放：音频内容展示
- **地图位置**：展示资源地理位置
- **收藏功能**：收藏/取消收藏资源
- **浏览量统计**：自动记录浏览量

#### 4. 地图（/map）
- **文化地标展示**：在地图上展示文化地标
- **交互功能**：点击地标查看详情
- **路线叠加**：预留路线展示功能

#### 5. 路线推荐（/routes, /route/:id）
- **路线列表**：展示推荐路线
- **主题筛选**：按主题筛选路线
- **路线详情**：查看路线详细信息
- **自定义路线**：根据用户偏好生成个性化路线

#### 6. 活动（/events, /event/:id）
- **活动列表**：展示文化活动列表
- **筛选功能**：
  - 状态筛选：即将开始、进行中、已结束、已取消
  - 类型筛选：展览、工作坊、演出、讲座等
  - 月份筛选：按月份筛选活动
- **活动详情**：查看活动完整信息
- **活动报名**：报名/取消报名活动
- **我的报名**：查看已报名的活动

#### 7. 社区（/community, /community/post/:id）
- **帖子列表**：展示社区帖子
- **排序功能**：按最新、热门等排序
- **帖子详情**：查看帖子完整信息及评论
- **发帖功能**：用户发布社区投稿（需审核）
- **互动功能**：
  - 点赞/取消点赞
  - 评论帖子
  - 收藏/取消收藏
- **我的内容**：查看我的帖子、点赞、评论、收藏记录

#### 8. 个人中心（/profile）
- **个人信息**：查看和编辑个人信息
- **收藏管理**：查看收藏的文化资源和帖子
- **投稿记录**：查看发布的社区投稿及审核状态
- **报名记录**：查看报名的活动

#### 9. 登录/注册（/login, /register）
- **用户注册**：支持用户名、邮箱注册
- **用户登录**：JWT Token 认证
- **路由守卫**：自动跳转到登录页（未登录访问受保护页面）
- **权限控制**：管理员权限验证

#### 10. 国际化（中/英）
- **语言切换**：支持中文和英文切换
- **持久化存储**：语言偏好本地存储
- **完整翻译**：所有页面和组件支持多语言

### 管理端主要功能（/admin）

#### 1. 仪表盘（AdminDashboard）
- **数据概览**：用户数、资源数、活动数、帖子数等统计
- **待审核内容**：待审核的社区投稿列表
- **进行中活动**：当前进行中的活动列表

#### 2. 用户管理（AdminUsers）
- **用户列表**：分页展示用户列表
- **搜索功能**：按关键词搜索用户
- **用户操作**：
  - 编辑用户信息
  - 启用/禁用用户
  - 删除用户

#### 3. 轮播图管理（AdminCarousels）
- **轮播图列表**：展示所有轮播图
- **创建/编辑**：添加或编辑轮播图
- **图片上传**：上传轮播图图片
- **排序控制**：设置轮播图显示顺序
- **启用/禁用**：控制轮播图显示状态
- **删除功能**：删除不需要的轮播图

#### 4. 文化资源管理（AdminCulture）
- **资源列表**：分页展示文化资源
- **搜索筛选**：按关键词、类型筛选
- **资源操作**：
  - 创建新资源
  - 编辑资源信息
  - 删除资源
- **文件上传**：上传资源图片、视频
- **多媒体管理**：管理资源的图片、视频、音频

#### 5. 活动管理（AdminEvents）
- **活动列表**：展示所有活动
- **搜索筛选**：按关键词、状态筛选
- **活动操作**：
  - 创建新活动
  - 编辑活动信息
  - 删除活动
- **报名管理**：
  - 查看活动报名列表
  - 审核报名（通过/驳回）
- **文件上传**：上传活动封面、图片、视频

#### 6. 社区投稿审核（AdminPosts）
- **帖子列表**：展示所有社区投稿
- **状态筛选**：按审核状态筛选（待审核、已通过、已驳回）
- **审核操作**：
  - 审核通过帖子
  - 驳回帖子（可填写驳回原因）
  - 删除违规帖子

#### 7. 首页推荐配置（AdminRecommendations）
- **推荐类型**：管理精选推荐（FEATURED）和热门推荐（HOT）
- **资源来源**：支持文化资源（CULTURE_RESOURCE）和社区投稿（COMMUNITY_POST）
- **推荐管理**：
  - 添加推荐配置
  - 更新推荐配置（显示顺序、启用状态）
  - 删除推荐配置
- **当前显示**：查看当前首页实际显示的资源及配置信息

---

## 🔧 配置说明

### 环境变量

创建 `.env` 文件：

```env
VITE_API_BASE_URL=http://localhost:8080/api
VITE_MAPBOX_TOKEN=your_mapbox_token
```

### API 代理配置

在 `vite.config.ts` 中配置了开发环境的 API 代理：

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, ''),
    },
  },
}
```

### 认证与权限

- 前端使用 Pinia 存储用户信息与 JWT Token
- Axios 拦截器自动附带 `Authorization: Bearer <token>`
- 受保护路由启用路由守卫（含管理员权限判断）
- Token 过期自动跳转到登录页

---

## 🌐 国际化

项目支持中文和英文两种语言，默认语言为中文。语言切换在 Header 组件中。

### 添加新语言

1. 在 `src/i18n/locales/` 目录下创建新的语言文件（如 `fr.ts`）
2. 在 `src/i18n/index.ts` 中导入并添加到 messages

示例：

```typescript
// src/i18n/locales/fr.ts
export default {
  common: {
    // 翻译内容
  }
}

// src/i18n/index.ts
import fr from './locales/fr'
// ...
messages: {
  zh,
  en,
  fr  // 添加新语言
}
```

---

## 🎨 样式规范

- **SCSS**：使用 SCSS 编写组件样式
- **Tailwind CSS**：使用 Tailwind CSS 进行快速样式开发
- **Element Plus**：遵循 Element Plus 设计规范
- **响应式设计**：
  - 移动端：< 768px
  - 平板：768px - 1024px
  - 桌面端：> 1024px

---

## 🔑 测试账号（本地初始化脚本）

如已执行后端数据库初始化脚本，可使用下列账号体验（建议仅在本地开发环境使用）：

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin  | admin123 | ADMIN |
| user1  | admin123 | USER |
| user2  | admin123 | USER |

---

## 📝 代码规范

### ESLint

```bash
npm run lint
```

### Prettier

```bash
npm run format
```

---

## 🧪 测试

```bash
npm run test
```

---

## 📦 部署

### GitHub Pages

1. 修改 `vite.config.ts` 中的 `base` 配置
2. 构建项目：`npm run build`
3. 部署 `dist` 目录到 GitHub Pages

### Netlify / Vercel

1. 连接 GitHub 仓库
2. 构建命令：`npm run build`
3. 发布目录：`dist`

### Docker

```dockerfile
FROM nginx:alpine
COPY dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

构建和运行：

```bash
docker build -t cultural-xinjiang-frontend .
docker run -p 80:80 cultural-xinjiang-frontend
```

---

## 🚧 开发注意事项

### 1. API 调用
- 所有 API 调用统一使用 `src/api/` 目录下的封装函数
- 使用 TypeScript 类型定义确保类型安全
- 错误处理统一在 Axios 拦截器中处理

### 2. 状态管理
- 用户状态使用 Pinia store (`src/store/user.ts`)
- 应用全局状态使用 Pinia store (`src/store/app.ts`)
- 避免在组件中直接使用 localStorage

### 3. 路由守卫
- 受保护路由需要设置 `meta.requiresAuth: true`
- 管理员路由需要设置 `meta.requiresAdmin: true`
- 路由守卫会自动处理权限验证

### 4. 组件开发
- 使用 Composition API 编写组件
- 组件命名使用 PascalCase
- 公共组件放在 `src/components/common/`
- 管理员组件放在 `src/components/admin/`

### 5. 样式开发
- 优先使用 Tailwind CSS 工具类
- 复杂样式使用 SCSS 编写
- 保持响应式设计，适配移动端

---

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

---

## 📄 许可证

MIT License

---

## 👥 作者

新疆数字文化平台开发团队

---

## 🙏 致谢

感谢所有为这个项目做出贡献的开发者和用户。
