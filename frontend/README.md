# 新疆数字文化平台 — 前端

## 📘 项目简介

新疆数字文化平台前端基于 **Vue 3 + Vite** 开发，面向游客、研究者及文化机构用户，提供新疆地区的数字文化展示与交互体验，包括文章、展品、视频、地图、活动与社区等功能。

系统采用 **模块化 + 响应式设计**，兼顾桌面端与移动端体验，支持多语言（中文/英文），与 Spring Boot 后端 REST API 对接，实现数据交互与用户认证。

- 管理员后台入口：`/admin`（需管理员权限）

## ⚙️ 技术栈

| 类别 | 技术 |
|------|------|
| 前端框架 | Vue 3 |
| 构建工具 | Vite |
| 组件库 | Element Plus |
| 路由管理 | Vue Router |
| 状态管理 | Pinia |
| 国际化 | vue-i18n |
| 网络请求 | Axios（带 JWT 拦截器） |
| 地图服务 | Mapbox / 高德地图 JS SDK |
| 多媒体展示 | Video.js、PhotoSwipe、Three.js（360° 展示） |
| 样式 | SCSS + Tailwind CSS |
| 测试 | Vitest + Vue Testing Library |
| 代码规范 | ESLint + Prettier |

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 7.0.0 或 yarn >= 1.22.0

### 安装依赖

```bash
npm install
# 或
yarn install
```

### 开发模式

```bash
npm run dev
# 或
yarn dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
# 或
yarn build
```

### 预览生产构建

```bash
npm run preview
# 或
yarn preview
```

## 📁 项目结构

```
frontend/
├── src/
│   ├── api/              # API 接口封装
│   ├── assets/           # 静态资源
│   ├── components/       # 公共组件
│   │   ├── common/       # 通用组件
│   │   └── layout/       # 布局组件
│   ├── i18n/             # 国际化配置
│   ├── layouts/          # 页面布局
│   ├── router/           # 路由配置
│   ├── store/            # Pinia 状态管理
│   ├── styles/           # 全局样式
│   ├── types/            # TypeScript 类型定义
│   ├── utils/            # 工具函数
│   ├── views/            # 页面组件
│   ├── App.vue           # 根组件
│   └── main.ts           # 入口文件
├── public/               # 公共静态资源
├── index.html            # HTML 模板
├── vite.config.ts        # Vite 配置
├── tsconfig.json         # TypeScript 配置
└── package.json          # 项目配置
```

## 🧭 功能说明

### 用户端主要功能

1. **首页（/home）** — 文化主题入口、轮播推荐、热门资源、最新更新
2. **搜索（/search）** — 关键词搜索、类型/地区/标签筛选
3. **详情页（/detail/:type/:id）** — 图片、视频、文字与地图位置、收藏
4. **地图（/map）** — 文化地标展示（预留路线叠加）
5. **路线推荐（/routes, /route/:id）** — 主题路线、生成自定义路线
6. **活动（/events, /event/:id）** — 活动列表/详情与报名
7. **社区（/community, /community/post/:id）** — 发帖、点赞、评论
8. **个人中心（/profile）** — 个人信息、收藏、投稿记录
9. **登录/注册（/login, /register）** — JWT 鉴权、路由守卫
10. **国际化（中/英）** — 语言切换与持久化

### 管理端主要功能（/admin）

- 仪表盘：概览关键数据
- 用户管理：启用/禁用、角色分配
- 轮播管理：新增、上下架、排序
- 文化资源管理：增删改查、推荐/热门开关
- 活动管理：发布、报名统计、状态管理
- 社区投稿审核：通过/驳回、违规处理

## 🔧 配置说明

### 环境变量

创建 `.env` 文件：

```env
VITE_API_BASE_URL=http://localhost:8080/api
VITE_MAPBOX_TOKEN=your_mapbox_token
```

### API 代理配置

在 `vite.config.ts` 中配置了开发环境的 API 代理：

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, '')，
    },
  },
}
```

### 认证与权限

- 前端使用 Pinia 存储用户信息与 JWT，Axios 拦截器自动附带 `Authorization: Bearer <token>`
- 受保护路由启用路由守卫（含管理员权限判断）

## 🌐 国际化

项目支持中文和英文两种语言，默认语言为中文。语言切换在 Header 组件中。

添加新语言：

1. 在 `src/i18n/locales/` 目录下创建新的语言文件
2. 在 `src/i18n/index.ts` 中导入并添加到 messages

## 🎨 样式规范

- 使用 SCSS 编写组件样式
- 使用 Tailwind CSS 进行快速样式开发
- 遵循 Element Plus 设计规范
- 响应式断点：768px（移动端/桌面端）

## 🔑 测试账号（本地初始化脚本）

如已执行后端数据库初始化脚本，可使用下列账号体验（建议仅在本地开发环境使用）：

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin  | admin123 | ADMIN |
| user1  | admin123 | USER |
| user2  | admin123 | USER |

## 📝 代码规范

### ESLint

```bash
npm run lint
```

### Prettier

```bash
npm run format
```

## 🧪 测试

```bash
npm run test
```

## 📦 部署

### GitHub Pages

1. 修改 `vite.config.ts` 中的 `base` 配置
2. 构建项目：`npm run build`
3. 部署 `dist` 目录到 GitHub Pages

### Netlify / Vercel

1. 连接 GitHub 仓库
2. 构建命令：`npm run build`
3. 发布目录：`dist`

### Docker

```dockerfile
FROM nginx:alpine
COPY dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

MIT License

## 👥 作者

新疆数字文化平台开发团队

## 🙏 致谢

感谢所有为这个项目做出贡献的开发者和用户。






